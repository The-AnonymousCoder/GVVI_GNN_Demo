# GVVI 四档分类 90% 准确率冲刺方案

## 0. 今晚的唯一目标

在固定测试节点不变、不使用测试标签、不伪造结果的前提下，将综合 GVVI 转换为四档分类，并尽最大可能使测试集精确分类 Accuracy 达到 90% 以上。

本方案不再追求“完全不使用任何已知 GVVI 的纯几何预测”，而是明确采用更容易成功、也符合当前 Demo 设定的任务：

> 已经精确计算研究区 85% 绿化网格的 GVVI，利用已知网格标签、空间邻接关系和廉价几何特征，补全剩余固定 15% 网格的 GVVI 档位。

这属于：

```text
半监督节点分类
+ 掩码标签建模
+ 图标签传播
```

不是跨城市零样本预测。最终汇报必须如实表述。

## 1. 当前结果审计

### 1.1 已有结果

当前报告中的最佳五档结果：

```text
精确Accuracy：73.1%
±1档Accuracy：96.7%
Spearman：0.779
```

说明模型大部分错误发生在相邻档，但距离 90% 精确分类仍有明显差距。

### 1.2 类别极不均衡

当前五档分布：

| 档位 | 占比 |
|---|---:|
| 0 | 26.5% |
| 1 | 51.3% |
| 2 | 19.5% |
| 3 | 2.5% |
| 4 | 0.2% |

前两类占 77.8%。因此：

- Accuracy 容易被低值多数类主导；
- 高值档只有极少样本；
- 即使总体 Accuracy 较高，也可能完全识别不了高值节点；
- 必须同时报告 Macro-F1、Balanced Accuracy 和每类召回率。

### 1.3 当前分类实现不可完全复现

当前仓库存在以下问题：

1. 报告写“五档加权CrossEntropy”，但提交的主训练代码是十档未加权CE加ordinal penalty；
2. 最佳五档/GVVI×100运行逻辑没有形成完整、独立、可一键运行的目录；
3. 当前十档 `_bin_gvvi` 使用全体节点标签计算分位点，测试标签参与了档位边界确定；
4. 十档最后一个边界使用非零数据100%分位数，导致最后一档接近零宽；
5. 报告和代码的类别定义不完全一致。

DeepSeek必须新建独立V4目录，不继续在V3文件上临时改动。

### 1.4 当前输入缺少决定分类的关键信息

现有特征能表达：

- 观察点数量；
- 距离；
- 大致方向；
- 建筑ID、道路ID和高度统计。

但仍不能表达：

- 相机与绿化之间真实建筑遮挡；
- 精确视线；
- 可见投影面积；
- 窗口和街道观察点对该绿化网格的真实贡献。

新增的“建筑遮挡代理”仍只是附近窗口/建筑统计，不是真正的射线遮挡。

另外必须修复：

- `normal_x/normal_y` 使用前必须单位化；
- “building_volume”不能简单用所有窗口对应建筑高度求和，否则同一建筑会因窗口数重复累计；
- 应按 `bldg_ID` 去重后再统计建筑高度。

### 1.5 当前图没有充分发挥作用

当前图：

- k=8空间近邻；
- k=24空间近邻；
- GraphSAGE只使用节点特征；
- 已经计算的 `edge_attr` 没有进入卷积。

因此模型不知道：

- 两节点相距多少；
- 是否为真实栅格邻接；
- 两节点观察环境是否相似；
- 哪类边更值得信任。

但不能用“真实GVVI相似”建立最终图，因为这会把测试答案写入图结构，属于标签泄漏。

## 2. 为什么单纯换GAT不一定达到90%

GAT可以学习不同邻居权重，但它只能重新加权已有信息：

```text
缺少视线/遮挡信息
→ GAT也无法凭空恢复
```

GAT最适合与“已知标签传播”结合：

```text
已知85%节点的精确档位
→ 作为图信号
→ GAT学习哪些已知邻居对当前未知节点最可靠
```

这比让GAT只看196维静态统计特征更有希望达到90%。

## 3. 四档定义

本轮不要保留“GVVI=0单独一档”，因为零与极低非零之间容易受渲染噪声和阈值影响，也是当前主要混淆来源。

四档定义必须只用训练标签确定，测试标签不得参与。

推荐使用训练集固定分位数：

```text
Class 0：最低60%
Class 1：60%—85%
Class 2：85%—97%
Class 3：最高3%
```

目的：

- 四档具有明确的“普通—较低—较高—热点”层次；
- 合并零值和微量值，消除最难且应用意义较弱的零/微量边界；
- 高值档仍保留，用于规划热点识别；
- 类别分布约为60/25/12/3，比当前五档更适合追求精确分类。

实现要求：

1. 只用85%已知标签中的训练部分计算三个阈值；
2. 将阈值保存为JSON；
3. 验证和测试只应用阈值，不能重新计算；
4. 报告真实四档分布；
5. 固定阈值后所有模型共用。

如果用户已经确定了另一套四档边界，必须先把边界写进配置并冻结，不能根据测试Accuracy反复调整。

## 4. 固定测试集与85%已知标签

原固定测试集：

```text
gvvi_grid_demo/processed/split_seed42.npz
test_idx = 4,146个节点
```

这4,146个测试节点永久未知，任何阶段都不能：

- 作为输入标签；
- 用于建标签相似边；
- 用于确定档位阈值；
- 用于选择模型或epoch；
- 用于校准概率。

其余85%节点视为已经精确计算GVVI。

在85%已知节点内部：

```text
80%上下文训练标签
5%验证查询标签
```

模型配置选定后，最终训练可以把原5%验证节点加入已知上下文，并使用前一阶段确定的最佳epoch，最终预测固定15%测试节点。

## 5. 最有希望成功的核心：掩码标签传播

### 5.1 节点输入

每个节点输入由两部分组成：

```text
A. 现有几何/观察点特征
B. 已知标签图信号
```

标签图信号：

```text
known_mask：该节点标签是否作为上下文公开
known_gvvi：已知则为精确连续GVVI，未知则为0
known_class_onehot：已知则为4档one-hot，未知则全0
```

测试节点始终：

```text
known_mask = 0
known_gvvi = 0
known_class_onehot = [0,0,0,0]
```

### 5.2 防止训练节点复制自己答案

如果训练时每个节点都看到自己的标签，模型会直接复制，形成严重泄漏。

必须采用随机掩码训练：

每个epoch：

1. 从85%已知节点中随机选20%—30%作为query；
2. query节点的标签输入全部清零；
3. 其余已知节点作为context；
4. 只在query节点上计算分类损失；
5. 验证和测试节点始终保持隐藏。

这相当于模拟：

```text
给定周围已知网格
→ 预测被遮住的未知网格
```

### 5.3 验证

验证时：

- 原80%训练节点作为context；
- 5%验证节点全部mask；
- 只计算5%验证Accuracy和Macro-F1；
- 测试节点仍完全隐藏。

### 5.4 最终测试

模型和epoch确定后：

- 85%非测试节点全部作为context；
- 15%测试节点全部mask；
- 输出测试四档。

这是今晚最可能显著提高准确率的合法方法。

## 6. 构图策略

### 6.1 图A：真实栅格邻接图

使用 `grid_idx_x/grid_idx_y` 建立 Queen 邻接：

```text
上下左右
+ 四个对角
```

只连接真实存在的绿化网格节点，不用欧氏kNN跨越空白区域。

这张图最适合局部标签传播。

### 6.2 图B：空间近邻图

保留：

```text
k=8
```

但增加最大距离阈值，避免跨越过大空白。

### 6.3 图C：观察环境相似图

基于标准化后的非标签特征建立：

```text
候选限制：空间距离≤250m
候选中选择特征余弦相似度最高的k=4
```

禁止使用GVVI、档位或可见像素计算相似度。

### 6.4 不建立“精确标签相似边”

可以单独做一个Oracle诊断：

```text
如果用真实GVVI相似度建边，理论上能达到多少？
```

但Oracle结果：

- 不能作为最终模型；
- 不能用于汇报Accuracy；
- 不能保存进最终预测图；
- 只能证明“边质量是瓶颈”。

今晚最终模型只能使用A/B/C三种无标签边。

## 7. 推荐模型：Label-Propagation GATv2

新模型：

> Masked Label-Propagation GATv2

结构：

```text
几何特征
→ Geometry Encoder（256维）

known_mask + known_gvvi + 4档one-hot
→ Label Encoder（64维）

拼接
→ 320维节点表示

图A：Queen邻接GATv2
图B：空间kNN GATv2
图C：观察环境相似GATv2

三个分支输出
→ Attention Fusion
→ Residual MLP
→ 4档logits
```

建议参数：

```text
GATv2 heads：4
每头维度：64
隐藏维度：256
层数：2
Dropout：0.15
Residual：开启
LayerNorm：开启
```

不要超过3层，避免标签传播过远和过平滑。

## 8. 边特征

GATv2必须实际接收边特征，而不是只计算后丢弃。

边特征：

```text
dx
dy
欧氏距离
是否Queen直接邻接
是否空间kNN
是否环境相似边
非标签特征余弦相似度
两节点观察点密度差
两节点方向暴露差
```

所有连续边特征只用训练统计量标准化。

不要加入：

- 两节点GVVI差；
- 是否同一真实档；
- 可见像素相似度；
- 测试标签信息。

## 9. 损失函数

类别分布不均衡，不能只用普通CrossEntropy。

建议：

```text
L = ClassBalancedFocalLoss
  + 0.2 × OrdinalDistanceLoss
```

ClassBalanced权重使用训练query标签统计，采用effective number：

```python
weight_c = (1 - beta) / (1 - beta ** n_c)
beta = 0.999
```

Focal gamma：

```text
gamma = 1.5
```

OrdinalDistanceLoss：

```text
预测概率的期望档位
与真实档位之间的SmoothL1
```

模型选择主指标：

```text
第一：验证集精确Accuracy
第二：验证集Macro-F1
第三：验证集Balanced Accuracy
```

## 10. 两个必须保留的基线

今晚不要尝试十几个模型，只保留：

### Baseline 1：邻居多数投票

对未知节点：

- 收集Queen邻居中已知节点；
- 按距离加权投票；
- 没有已知邻居时使用kNN已知节点。

这个基线可能已经很强，且能判断空间标签传播能达到什么水平。

### Baseline 2：现有几何分类模型

保留当前不输入已知标签的四档GraphSAGE结果。

最终对比：

```text
几何GraphSAGE
邻居标签投票
Masked Label-Propagation GATv2
```

这不是大规模消融，而是证明90%来自什么。

## 11. 今晚的执行顺序

### 阶段1：先验证90%是否现实

不训练GAT，先运行邻居标签投票。

若固定测试集Accuracy：

```text
≥90%：说明空间标签传播足够，立即实现GAT并完成Demo
80%—90%：GAT有机会超过90%
<80%：说明空间连续性不足，四档精确90%不现实
```

这一阶段预计几分钟完成。

### 阶段2：Masked GATv2

实现随机掩码训练，先跑：

```text
500节点冒烟
→ 全量300epoch
→ 验证集选择
```

如果验证Accuracy继续上升，再延长到800epoch；不要一开始跑1500epoch。

### 阶段3：最终85%标签上下文测试

冻结模型配置与epoch：

- 85%非测试节点作为已知context；
- 15%固定测试节点未知；
- 评估一次；
- 输出最终结果。

## 12. 90%目标的诚实判定

必须分别报告：

- Exact Accuracy；
- ±1档Accuracy；
- Macro-F1；
- Weighted-F1；
- Balanced Accuracy；
- 每类Precision/Recall/F1；
- 混淆矩阵。

禁止把：

```text
±1档Accuracy 96.7%
```

写成：

```text
分类Accuracy 96.7%
```

如果最终：

```text
Exact Accuracy ≥90%
Macro-F1 ≥0.70
```

可以汇报：

> 在85%网格已精确计算的条件下，利用掩码标签传播GATv2补全剩余15%网格，四档精确分类准确率超过90%。

如果只有±1档超过90%，必须汇报：

> 精确分类准确率为X%，相邻一档容差准确率为Y%。

## 13. 新目录

```text
gvvi_grid_demo_v4/
├── config.yaml
├── requirements.txt
├── run_all.py
├── src/
│   ├── define_classes.py
│   ├── freeze_split.py
│   ├── build_features.py
│   ├── build_three_graphs.py
│   ├── neighbor_vote.py
│   ├── masked_dataset.py
│   ├── gatv2_model.py
│   ├── losses.py
│   ├── train.py
│   ├── evaluate.py
│   └── export.py
├── processed/
├── checkpoints/
├── outputs/
│   ├── metrics.json
│   ├── predictions.csv
│   ├── pred_class.tif
│   └── figures/
└── reports/
    ├── audit.md
    └── final_report.md
```

## 14. DeepSeek必须先修复的问题

在新实验前：

1. 四档边界只用训练标签计算；
2. 保存阈值和类别分布；
3. 归一化 `normal_x/normal_y`；
4. 建筑统计按 `bldg_ID` 去重；
5. 不把“建筑高度求和”称为建筑体积；
6. 确保GATv2实际使用edge_attr；
7. 把最佳五档临时代码整理为可复现文件，或者明确废弃；
8. 所有指标能从predictions.csv独立复算。

## 15. 禁止事项

禁止：

- 用测试GVVI建边；
- 用测试标签定义分档阈值；
- 为达到90%反复修改测试集；
- 把多数类占比设计到90%以上后宣称模型成功；
- 删除边界附近难样本；
- 只评估有已知邻居的测试节点；
- 把测试节点标签作为输入；
- 把±1档准确率冒充精确准确率；
- 使用颜色ID、可见像素、`total_pixel`作为输入；
- 伪造日志、指标或图。

## 16. 最终交付

DeepSeek必须提供：

1. 固定4,146个测试节点的SHA256；
2. 四档阈值及其训练来源；
3. 四档全体/训练/验证/测试分布；
4. 邻居投票基线结果；
5. GATv2最终结果；
6. Exact Accuracy、Macro-F1、Balanced Accuracy；
7. 每类指标和混淆矩阵；
8. `predictions.csv`；
9. `pred_class.tif`；
10. GPU命令、最佳epoch、时间和显存；
11. 最新Git commit；
12. 是否真正达到90%；
13. 未达到时最主要的证据和原因。

所有代码、指标、图表和报告必须commit并push。

